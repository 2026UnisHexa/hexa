# Anthropic API key — used ONLY by the Vercel serverless function (api/suggest-chords).
# Never prefix with VITE_ (that would expose the key to the browser bundle).
ANTHROPIC_API_KEY=sk-ant-your-key-here
